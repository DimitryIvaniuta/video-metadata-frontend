mutation CreateTicket($input: TicketCreateInput!) {
    createTicket(input: $input) {
        id
        title
        status
        priority
        reporterId
        assigneeId
        createdAt
        updatedAt
    }
}
