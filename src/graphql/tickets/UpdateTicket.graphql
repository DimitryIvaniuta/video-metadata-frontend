mutation UpdateTicket($input: TicketUpdateInput!) {
    updateTicket(input: $input) {
        id
        title
        status
        priority
        assigneeId
        assigneeUsername
        updatedAt
    }
}
